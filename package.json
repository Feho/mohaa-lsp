{
  "name": "mohaa-lsp-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Language Server Protocol implementation for MOHAA Morpheus Script",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "package": "pnpm --filter morpheus-vscode package"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
